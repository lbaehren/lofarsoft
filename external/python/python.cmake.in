
set (CMAKE_INSTALL_PREFIX ${USG_INSTALL_PREFIX} CACHE PATH "Installation prefix" FORCE)
set (USG_ROOT             ${USG_ROOT}           CACHE PATH "Top of code tree"    FORCE)

set (BOOST_INCLUDES    ${BOOST_INCLUDES}    CACHE  PATH     "Boost headers"    FORCE)
set (BOOST_LIBRARIES   ${BOOST_LIBRARIES}   CACHE  FILEPATH "Boost libraries"  FORCE)

set (FFTW3_INCLUDES    ${FFTW3_INCLUDES}    CACHE  PATH     "FFTW3 headers"    FORCE)
set (FFTW3_LIBRARIES   ${FFTW3_LIBRARIES}   CACHE  FILEPATH "FFTW3 libraries"  FORCE)

set (GLIB_INCLUDES     ${GLIB_INCLUDES}     CACHE  PATH     "GLIB headers"     FORCE)
set (GLIB_LIBRARIES    ${GLIB_LIBRARIES}    CACHE  FILEPATH "GLIB libraries"   FORCE)

set (PYTHON_INCLUDES   ${PYTHON_INCLUDES}   CACHE  PATH     "Python headers"   FORCE)
set (PYTHON_LIBRARIES  ${PYTHON_LIBRARIES}  CACHE  FILEPATH "Python libraries" FORCE)

set (NUMPY_INCLUDES   ${NUMPY_INCLUDES}   CACHE  PATH     "NumPy headers"   FORCE)
set (NUMPY_LIBRARIES  ${NUMPY_LIBRARIES}  CACHE  FILEPATH "NumPy libraries" FORCE)
set (NUMPY_VERSION     ${NUMPY_VERSION}     CACHE STRING "NumPy version")
set (NUMPY_DIRECTORY   ${NUMPY_DIRECTORY}   CACHE PATH   "NumPy source directory")
set (NUMPY_TAR_ARCHIVE ${NUMPY_TAR_ARCHIVE} CACHE STRING "NumPy tar-archive")
