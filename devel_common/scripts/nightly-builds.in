#!/bin/bash

echo "-- Set environment variables ..."

export LOFARSOFT=${USG_ROOT}
. $LOFARSOFT/devel_common/scripts/init.sh

echo "-- Cleaning up code base ..."

cd $LOFARSOFT
rm -rf release
rm -rf build

echo "-- Updating working copy ..."

svn up

echo "-- Building projects ..."

cd build
./bootstrap

make cr

echo "-- Experimental build of selected projects ..."

cd $LOFARSOFT/build/dal && make Nightly
cd $LOFARSOFT/build/cr && make Nightly
